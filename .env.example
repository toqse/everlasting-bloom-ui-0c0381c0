# Site mode: development | testing | production
# Optional: TestingBanner is no longer rendered in layout.
NEXT_PUBLIC_SITE_MODE=production

# Log every API request in the browser console after deploy (build-time flag).
NEXT_PUBLIC_API_DEBUG=true
